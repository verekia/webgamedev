import { ManapotionLogo } from 'lib/logos'

# 🕹️ Controls

## Keyboard

The browser's `keydown` and `keyup` events let you track which keys are pressed. For movement, games typically maintain a set of currently held keys and poll it each frame rather than responding to individual events.

Using `event.code` (e.g., `"KeyW"`, `"Space"`) instead of `event.key` avoids layout issues — a player on an AZERTY keyboard won't have `W` where you expect it. See the [i18n keyboard controls guide](https://hacks.mozilla.org/2017/03/internationalize-your-keyboard-controls/) and the [Keyboard.getLayoutMap()](https://developer.mozilla.org/en-US/docs/Web/API/Keyboard/getLayoutMap) API for remapping support.

## Mouse

For first-person or camera-rotation controls, the [Pointer Lock API](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_Lock_API) captures the cursor and provides raw delta values (`movementX`, `movementY`) — the cursor no longer hits the screen edge.

Scroll events are inconsistent across browsers and devices (trackpads vs. mouse wheels produce wildly different delta values), so normalization is often needed.

## Gamepad

The [Gamepad API](https://developer.mozilla.org/en-US/docs/Web/API/Gamepad_API/Using_the_Gamepad_API) provides access to connected controllers, exposing analog stick values and button states. It is a **polling** API — you read `navigator.getGamepads()` each frame rather than listening to events. Analog sticks have natural drift and need a deadzone threshold applied before use.

## Mobile

On touchscreens, virtual joysticks are the most common control scheme. They handle touch events and return normalized direction vectors and force values.

## Resources

- <ManapotionLogo /> [Mana Potion](https://manapotion.org)
- [Ecctrl](https://github.com/pmndrs/ecctrl)
- [Drei KeyboardControls](https://github.com/pmndrs/drei#keyboardcontrols)
- [Drei PointerLockControls](https://github.com/pmndrs/drei#controls)
- [Drei ScrollControls](https://github.com/pmndrs/drei#scrollcontrols)
- [normalize-wheel](https://github.com/basilfx/normalize-wheel)
- [joypad.js](https://github.com/ArunMichaelDsouza/joypad.js)
- [nipplejs](https://www.npmjs.com/package/nipplejs)
- [Rapier's Character Controller](https://rapier.rs/docs/user_guides/javascript/character_controller)
- [meshwalk](https://github.com/yomotsu/meshwalk)
