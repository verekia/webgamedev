# ⚡ Performance

## Asset Optimization

3D assets are often the biggest performance bottleneck. The **glTF** format (`.gltf` / `.glb`) is the standard for web 3D and supports **Draco** compression for geometry and **KTX2** for textures, both of which drastically reduce file sizes and GPU memory usage. Tools like gltfpack and meshoptimizer can further optimize meshes by simplifying geometry and improving GPU cache efficiency.

Reuse geometries and materials across meshes whenever possible — creating new ones for each object wastes memory and prevents batching.

## Draw Calls

Every time the GPU draws a group of triangles with a given material, that's a **draw call**. Each one has CPU overhead, so reducing the total count is one of the most impactful optimizations. Techniques include:

- **Batching** — combining multiple meshes that share a material into a single draw
- **Instancing** — drawing many copies of the same geometry in one call, each with different transforms
- **Merging** — baking multiple static meshes into one geometry at build time

## Level of Detail (LOD)

LOD swaps high-poly models for simpler versions as objects get farther from the camera. At a distance, a character might go from 10,000 triangles to 500 — the player won't notice, but the GPU will.

## Culling

Don't render what isn't visible. **Frustum culling** (built into most engines) skips objects outside the camera's view. **Occlusion culling** skips objects hidden behind other objects. **Distance culling** removes objects beyond a threshold — often paired with fog to hide the cutoff.

## Shadows

Shadows are expensive. Bake static shadows into lightmaps where possible. For dynamic shadows, limit the shadow-casting distance and the number of lights that cast shadows. Simpler shadow techniques (blob shadows, projected textures) can look good enough at a fraction of the cost.

## Materials

Simpler materials are cheaper. An unlit material is faster than a physically-based one. If an object doesn't need lighting, don't compute it. Choose the simplest material that achieves the look you need.

## Spatial Partitioning

For collision detection or querying nearby objects, iterating over every entity is wasteful. **Spatial hashing**, **octrees**, and **BVH** (Bounding Volume Hierarchies) partition space so you only check objects that are close together.

## Profiling

Don't optimize blindly. Use your browser's performance profiler (Chrome DevTools Performance tab, Firefox Profiler) to find actual bottlenecks. Measure GPU time vs. CPU time — they have different solutions.

## JavaScript Patterns

In hot loops (code running every frame for many entities), micro-optimizations matter:

- Prefer `for` loops over `.forEach` / `.map` — they avoid function call overhead and allocations
- Mutate objects in place (`Object.assign`, set properties directly) rather than creating new ones with spread (`{ ...obj }`)
- Avoid allocating new objects, arrays, or vectors per frame — reuse them

## Resources

- [gltf-pipeline](https://github.com/CesiumGS/gltf-pipeline)
- [gltf-transform](https://github.com/donmccurdy/glTF-Transform)
- [glTF-Compressor](https://github.khronos.org/glTF-Compressor-Release/) by [Khronos](https://www.khronos.org/)
- [Blender mesh-gpu-instancing](https://github.com/takahirox/glTF-Blender-IO-EXT-mesh-gpu-instancing) ([demo](https://www.reddit.com/r/threejs/comments/yw1yw8/2000_chairs_created_with_blenders_geometry_nodes/))
- [Three.js LOD](https://threejs.org/docs/#api/en/objects/LOD) (Drei [Detailed](https://github.com/pmndrs/drei#detailed))
- [PerspectiveCamera.far](https://threejs.org/docs/index.html#api/en/cameras/PerspectiveCamera.far) + [Fog](https://threejs.org/docs/#api/en/scenes/Fog)
- [Spatial hashing](https://www.youtube.com/watch?v=sx4IIQL0x7c) ([Yuka](https://github.com/Mugen87/yuka/tree/master/src/partitioning), [other video](https://www.youtube.com/watch?v=i0OHeCj7SOw))
- Drei [Performance Monitor](https://github.com/pmndrs/drei#performancemonitor)
- [Fixed update](https://github.com/pmndrs/react-three-fiber/issues/2338)
- [How Games Use "Culling" to Be More Efficient](https://www.youtube.com/watch?v=CHYxjpYep_M) by SimonDev
- [Three.js Optimization - Best Practices and Techniques](https://www.youtube.com/watch?v=dc5iJVInpPY)
- [PlayCanvas's guidelines](https://developer.playcanvas.com/en/user-manual/optimization/guidelines/)
- [Tojiro's WebXR Scene Optimization article](https://toji.dev/webxr-scene-optimization/)
- [Optimizing scenes for better WebGL performance by Verge3D](https://www.soft8soft.com/docs/manual/en/introduction/Optimizing-WebGL-performance.html)
- [Meta WebXR optimizations talk](https://web.facebook.com/watch/?v=573587777089806)
- [Bullet Hell article (Unity)](https://blog.littlepolygon.com/posts/bullets/)
