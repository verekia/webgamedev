# 📋 Instanced Meshes

If you have multiple identical meshes in your scene, use instanced meshes to reduce the number of draw calls. This is especially useful for large scenes with many trees, rocks, or other objects that are repeated many times.

## Drei `Instances` and `Merged`

If using React Three Fiber with Drei, you can use `<Instances />`:

```jsx
import { Instance, Instances } from '@react-three/drei'

import { treeGeometry, treeMaterial } from 'lib/resources'

const trees = [
  { id: 1, position: [0, 0, 0] },
  { id: 2, position: [1, 0, 0] },
  { id: 3, position: [2, 0, 0] },
]

const Trees = () => (
  <Instances geometry={treeGeometry} material={treeMaterial}>
    {tree.map(t => (
      <Instance key={t.id} position={t.position} />
    ))}
  </Instances>
)
```

If you need multiple instanced meshes per entity, you can use `<Merged />`:

```jsx
import { Merged } from '@react-three/drei'

import { leavesGeometry, leavesMaterial, trunkGeometry, trunkMaterial } from 'lib/resources'

const trees = [
  { id: 1, position: [0, 0, 0] },
  { id: 2, position: [1, 0, 0] },
  { id: 3, position: [2, 0, 0] },
]

const Trees = () => (
  <Merged meshes={[leaves, trunk]}>
    {(Leaves, Trunk) => (
      <>
        {tree.map(t => (
          <Fragment key={t.id}>
            <Leaves position={[t.position[0], t.position[1] + 5, t.position[2]]} />
            <Trunk position={t.position} />
          </Fragment>
        ))}
      </>
    )}
  </Merged>
)
```

This works great for static meshes, but not for animated skinned meshes.

## Instanced Skinned Meshes

Instanced skinned meshes (animated characters rendered via instancing) are not straightforward in Three.js. WebGPU has native support, while WebGL requires workarounds. One technique from [NVIDIA GPU Gems](https://developer.nvidia.com/gpugems/gpugems3/part-i-geometry/chapter-2-animated-crowd-rendering) encodes all frames of all animations into a texture and looks up bone matrices from that texture in the vertex shader.

### Resources

- [WebGL instanced skinning example](https://github.com/wizgrav/three.js/blob/dev/examples/webgl_instancing_skinning.html) ([demo](https://wizgrav.github.io/three.js/examples/webgl_instancing_skinning.html), [closed unmerged PR](https://github.com/mrdoob/three.js/pull/22667))
- [WebGPU skinning instancing example](https://github.com/mrdoob/three.js/blob/dev/examples/webgpu_skinning_instancing.html) ([demo](https://threejs.org/examples/webgpu_skinning_instancing.html) — requires WebGPU enabled)
- [Cody Bennett's implementation](https://codesandbox.io/s/three-js-instanced-skinned-mesh-2yfgiu)
- [InstancedBatchedSkinnedMesh](https://jsfiddle.net/cbenn/Las0poyu) by Cody Bennett
- [iizuka masayuki's demo](https://112ka.github.io/sketches/instanced_skinned_mesh/) ([source](https://github.com/112KA/Sketches/tree/main/apps/sketches/src/instanced_skinned_mesh))
- [Three.js forum discussion on optimizing large amounts of skinned meshes](https://discourse.threejs.org/t/optimization-of-large-amounts-100-1000-of-skinned-meshes-cpu-bottlenecks/58196)
